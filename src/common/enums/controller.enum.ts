export enum ControllerEnum {
  USER = 'users',
  AUTH = 'auth',
  CONVERSATION = 'conversations',
  MESSAGE = 'messages',
  CATEGORY = 'categories',
  MENU = 'menus',
  BANK = 'banks',
  CARD = 'cards',
  SCHEDULE = 'schedules',
}
